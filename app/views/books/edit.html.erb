
<div class="container">
    <div class="row">
<% if @book.errors.any? %>
  <div id="error_explanation">
    <h2><%= @book.errors.count %>errors prohibited this book from being saved:</h2>
    <ul>
    <% @book.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
        <div class="col-xs-3">
<%= form_for(@book) do |f| %>
    <h2>Editing Book</h2>
    <Label>Title</Label>
        <%= f.text_field :title, autofocus: true, class:"form-control", id:"inputEmail", placeholder:""%>
    <label>Body</label>
        <%= f.text_area :body, autofocus: true, class:"form-control", id:"inputEmail", placeholder:""%>
    <%= f.submit "Update Book", class:"btn btn-lg btn-block btn-base-1 mt-20" %>
<% end %>
<%= link_to "Show", book_path(@book) %>
<%= link_to "Back", books_path %>
</div>
</div>
</div>
